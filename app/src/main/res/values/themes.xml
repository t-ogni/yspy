<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Шпион" parent="Theme.AppCompat.DayNight.NoActionBar">

    </style>
    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/mainColor</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:radius">8dp</item>
    </style>
    <style name="CustomListView" parent="Widget.AppCompat.ListView">
        <item name="android:textColor">@android:color/white</item>

    </style>
    <style name="Theme.Шпион" parent="Base.Theme.Шпион" />
</resources>